version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: mysql-secorvi
    environment:
      MYSQL_ROOT_PASSWORD: 2037888
      MYSQL_DATABASE: secorvi_db
    ports:
      - "3306:3306"
    volumes:
      - ./init:/docker-entrypoint-initdb.d